// REPLACE ALL
const str1 = "vothana is the best of the best"
console.log(str1.replaceAll('best', 'worst'));

// REPLACE
// older method, only replaces first instance
const str2 = "abababab"
console.log(str2.replaceAll('b', 'c'));
console.log(str2.replace('b', 'c'));
